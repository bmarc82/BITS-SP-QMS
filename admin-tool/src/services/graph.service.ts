/**
 * Microsoft Graph Service
 */

export class GraphService {
  async getSiteInfo(siteUrl: string): Promise<object> {
    throw new Error('Nicht implementiert');
  }

  async getUserInfo(userId: string): Promise<object> {
    throw new Error('Nicht implementiert');
  }
}
